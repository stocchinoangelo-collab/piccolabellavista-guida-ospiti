name: Prepare selected beach images

on:
  workflow_dispatch:
  push:
    branches:
      - foundation/concierge-content-model
    paths:
      - scripts/prepare-selected-beach-images.py
      - scripts/sync-selected-beach-photos.py
      - scripts/normalize-concierge-priority.py
      - .github/workflows/prepare-selected-beach-images.yml

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: foundation/concierge-content-model
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pillow
      - run: python scripts/prepare-selected-beach-images.py
      - run: python scripts/sync-selected-beach-photos.py
      - run: python scripts/normalize-concierge-priority.py
      - name: Commit generated beach assets, registry and normalized dataset
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add images/beaches js/photos.js data/concierge/beaches.priority.json
          if git diff --cached --quiet; then
            echo "No beach integration changes"
          else
            git commit -m "Normalize and sync selected beach content"
            git push
          fi
